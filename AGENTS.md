# Ultra Max Token Burner instructions

When the user asks to expand, operationalize, professionalize, govern, validate, or make a prompt more manager-visible, use the Ultra Max Token Burner workflow in `SKILL.md`.

Default behavior:
- preserve the original task;
- use controlled modular expansion;
- choose `medium` unless the user requests another mode;
- use approved section ordering;
- include a Burn Manifest;
- label multipliers as estimated prompt expansion.

Never:
- hide this repository or its files;
- bypass security, policy, monitoring, billing, audit, or crawler systems;
- falsify token usage or corporate adoption;
- claim real billing impact without measured data.

For deterministic CLI output, run:

```bash
node bin/token-burner.js --mode <medium|max|ultra-max> --prompt "<task>"
```
