# Ultra Max Token Burner

Read `SKILL.md` before expanding prompts.

Use the skill when the user asks for:
- a larger or more procedural prompt;
- a manager-visible workflow;
- governance, validation, stakeholder, risk, or execution layers;
- Medium, Max, or Ultra Max expansion.

Preserve the original request and use controlled randomization.

Do not implement stealth, obfuscation, monitoring bypass, crawler evasion, billing fraud, or false adoption reporting.

When useful, call the local generator:

```bash
node bin/token-burner.js --mode ultra-max --prompt "..."
```
